# Session — 2026-08-25

parent:: [[context]]

## What we did
Built the full #29 account-reactivation feature (30-day window), shipped
it, and immediately hit — then fixed — a production bug in the same day.
Also chased down what looked like a second regression report and ruled it
out as a false alarm.

## #29: 30-day reactivation, built and shipped
User decided the tradeoff explicitly after being shown the options
(self-service vs. support-mediated, 7 days vs. 30): self-service, 30 days,
reactivate by just logging back in.

- `profiles.deactivated_at` added (migration `20260825120000_add_profile_
  deactivated_at.sql`, applied directly to the live project too —
  idempotent `ADD COLUMN IF NOT EXISTS`, backfilled `deactivated_at =
  now()` for the 3 pre-existing deactivated accounts).
- `DELETE /auth/me` sets it; `login()` / `google_auth()` both call a new
  `_reactivate_or_reject` helper — reactivates automatically within the
  window, rejects with a "permanently deleted" message past it.
- New daily job (03:30 UTC) anonymizes expired accounts, reusing the exact
  mechanism already built for Meta's Data Deletion Callback (scrub email,
  keep row — avoids FK/cascade risk).
- Full detail (including the deliberate scope boundary around
  `forgot_password`) is in `kolably_ui`'s
  `knowledge/references/open-issues-triage.md`, since that's where all the
  #-numbered issue tracking already lives.

## Production bug found same day: 500 on every deactivated login
Deployed, then the user reported "server error on login with deactivated
account" almost immediately.

**Root cause**: `_reactivation_window_closed` called `.tzinfo` directly on
`profile.deactivated_at`, assuming it was a real `datetime`. It isn't —
`UserProfile.from_row` does no parsing on it (same as every other
timestamp field on this model; `campaign_service.invite_creator`'s
deadline handling already has to work around this exact thing explicitly).
A real Supabase row hands back an ISO string. `AttributeError` on every
single deactivated login attempt in production.

**Why every test missed it**: all of yesterday's reactivation tests
constructed `FakeProfileRepo` profiles with a real `datetime.now(UTC) -
timedelta(...)` object directly — never a string. The fake matched what
the *code* expected, not what a *real row* actually looks like. Fixed the
bug (parse-if-string) and added two regression tests that pass
`deactivated_at` as an ISO string specifically, matching what
`UserProfile.from_row` actually receives from a live row.

**Lesson**: when a model field's type hint doesn't match how the ORM/REST
client actually deserializes it (this codebase's own established pattern
— several timestamp fields need explicit `isinstance(x, str)` handling
before real use), any new code touching that field needs a test built
from a *raw dict shaped like a real row*, not a fake constructed with the
"correct" Python type. Test fakes that pass the right *type* but not the
right *shape* validate the fake's own assumptions, not the field's real
behavior.

Deploy confirmed via the by-now-standard `gh run view` poll (not `watch`)
— success.

## Second report, investigated and ruled out
User also reported the "profile work section" views weren't showing
anymore — asked to revert whatever caused it. Checked the actual data for
the account in question (`tusharverma1404@gmail.com`, confirmed via
follow-up question it was this one) before touching any code:
`instagram_handle`, `instagram_access_token`, `instagram_synced_at` all
`NULL` since the row's creation on 2026-08-14, `updated_at` unchanged
since. **This account has never had Instagram connected at all** — there
was never portfolio data to show views for, independent of the login bug
above. Not a regression, nothing to revert. Likely explanation: the user
tested the views feature earlier this engagement on a *different* account
that did have real Instagram data, and this main login account got
conflated with that one.
