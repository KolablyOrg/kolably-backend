# Session — 2026-08-24

parent:: [[context]]

## What we did
Continuation of the same live-notifications debugging session from
[[logs/2026-08-23]] (date rolled over mid-session). Root-caused and fixed
why live notifications (and, it turns out, chat too) never actually
delivered live in production.

## Decisions / fixes made
- Root cause #1: `getSupabase()` in `kolably_ui/src/lib/supabase.ts`
  returned `null` in production because `VITE_SUPABASE_URL` /
  `VITE_SUPABASE_ANON_KEY` were never configured as Vercel Environment
  Variables (`.env` is gitignored, so Vercel's build never had them).
  User added them to Vercel and redeployed.
- Root cause #2 (found immediately after #1 was fixed, via the
  `[notif-debug]` logging): `kolably_ui/vercel.json`'s CSP `connect-src`
  never listed the Supabase project origin, so the browser blocked the
  Realtime WebSocket outright (`CHANNEL_ERROR: transport failure`). Fixed
  by adding `https://uxngcuyrdmajydkqpiyi.supabase.co` and
  `wss://uxngcuyrdmajydkqpiyi.supabase.co` to `connect-src`. Committed
  `da310a2`, merged with unrelated concurrent mobile/README commits, pushed
  as `b7d034b`.
- Both findings written up in
  [[knowledge/references/kolably-ui-missing-supabase-env-vars]] — **this
  silently broke chat's live delivery too**, not just notifications; chat's
  REST resync fallback was masking it the whole time.

## Knowledge saved
- [[knowledge/references/kolably-ui-missing-supabase-env-vars]] — the two
  stacked root causes and why both had to be fixed together.

## Context changes
Linked the new knowledge note from [[context]].

## Open questions
- Not yet confirmed end-to-end: does the user now actually see a
  notification pop live after both fixes? Awaiting confirmation before
  marking [[decisions/002-notifications-realtime-via-supabase]] as fully
  working (currently just "implemented + applied," not "confirmed live").
- The temporary `[notif-debug]` console logging in
  `useNotificationsRealtime.ts` is still deployed — remove it once live
  delivery is confirmed working.
- Still open from [[logs/2026-08-23]]: `NotificationBell` Overview-only
  mounting scope, and the RLS-disabled advisory on
  `collaboration_revision_history`/`business_shortlists`.

## Next steps
- Get user confirmation that the test notification (or a fresh one) pops
  live now.
- Remove `[notif-debug]` logging once confirmed.
- Ask the user whether they also want to verify chat's live delivery now
  that the same underlying bugs are fixed.
