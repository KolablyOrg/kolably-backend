# Session — 2026-08-23

parent:: [[context]]

## What we did
Initialized the `.claudevault/` knowledge vault for `kolably_backend`.
Scaffolded the directory structure and seeded `context.md` from a scan of
the repo: `README.md` (stack, architecture, key tables), `docs/PROJECT_STATUS.md`
(module implementation status), `.specify/memory/constitution.md` (newly
ratified project constitution), and recent git history. Also reviewed the
sibling `TESTING_AUTOMATION_PLAN.md` (one directory up, spans this repo and
`kolably_ui`) since it was open in the user's editor, and saved it as a
knowledge reference.

## Decisions made
- [[decisions/001-constitution-ratified]] — documented the pre-existing
  ratification of Constitution v1.0.0 (commit `d859ec3`), not a new decision
  made this session but captured now since it wasn't yet in the vault.

## Knowledge saved
- [[knowledge/references/testing-automation-plan]] — cross-repo testing/CI
  roadmap; relevant to future work on `tests/` and `.github/workflows/`.
- [[knowledge/references/notification-architecture]] — full sweep of the
  notification system (in-app CRUD is complete; live/push delivery is
  poll-only by design, chat is the only channel with Supabase Realtime
  wired). Also resolves the chat open question below.

## Context changes
Created `context.md` from scratch (overview, stack, architecture, module
status table, related-repo pointer to the testing automation plan). Later in
the session, updated the module status table: Chat flipped to ✅ (confirmed
implemented, including Realtime broadcast) and added a Notifications row
(✅ CRUD/polling, ❌ no live push) — see
[[knowledge/references/notification-architecture]].

## Open questions
- ~~`docs/PROJECT_STATUS.md` says Chat is "❌ Not implemented" but a Supabase
  Realtime broadcast migration for chat messages was added 2026-08-22
  (commit `0d3a2e8`)~~ — **resolved this session**: chat is implemented,
  including the Realtime broadcast trigger; `docs/PROJECT_STATUS.md` is
  stale. See [[knowledge/references/notification-architecture]].

## Next steps
- If the webapp needs a live notification bell (not just chat), the fastest
  path is to replicate the chat Realtime broadcast pattern for the
  `notifications` table — see [[knowledge/references/notification-architecture]]
  for the specific migration to mirror.
- **Done later this session**: wrote that migration — see
  [[decisions/002-notifications-realtime-via-supabase]]. Still needed: (1)
  apply `migrations/20260823150000_notifications_realtime_broadcast.sql` to
  the live Supabase project, (2) add webapp-side subscription code to
  `notifications:{profileId}`.
- **Blocked**: attempted to apply the migration via the Supabase MCP tool,
  but discovered it's connected to an unrelated project ("LIFT", a fitness
  app) rather than kolably_backend's real project — see
  [[knowledge/references/supabase-mcp-project-mismatch]]. User is
  reconnecting Supabase MCP to the correct project; re-verify via
  `list_projects` before calling `apply_migration` again.
- **Done**: (2) webapp subscription code, in the sibling repo `kolably_ui`
  (not this repo, but part of the same [[decisions/002-notifications-realtime-via-supabase]]
  work) — new `src/lib/notificationRealtime.ts` and
  `src/hooks/useNotificationsRealtime.ts` mirroring the existing chat
  realtime files 1:1, wired into `src/components/dashboard/NotificationBell.tsx`.
  Typechecked (`tsc --noEmit`) and linted clean.
- **Unblocked and done**: user reconnected Supabase MCP to the correct
  project; verified via `list_projects`/`list_tables` before proceeding
  (per [[knowledge/references/supabase-mcp-project-mismatch]]'s rule), then
  applied `migrations/20260823150000_notifications_realtime_broadcast.sql`
  via `apply_migration` and confirmed live (trigger + RLS policy exist).
  [[decisions/002-notifications-realtime-via-supabase]] is now fully
  implemented end-to-end (DB trigger, RLS, webapp subscription).
- **Found while verifying, not part of this task — surfaced to user,
  not auto-fixed**: Supabase advisory flagged `public.collaboration_revision_history`
  and `public.business_shortlists` as having RLS **disabled** (fully exposed
  to anon/authenticated via the client libraries). Presented the
  remediation SQL to the user; did not run it, since enabling RLS without
  policies would block all access to those tables — needs the user to
  decide on policies first.

- **Pushed to `main` in both repos**: user asked to push. Committed only
  the notification-realtime files by explicit pathspec (not `git add -A`)
  in both `kolably_backend` and `kolably_ui`, leaving several other
  sessions' concurrent uncommitted changes (migration renames, `deploy.yml`,
  `collaboration_service.py`, new untracked migrations, `tests_integration/`,
  `.env.e2e-local*`, etc.) completely untouched. Both pushes were rejected
  (remotes had moved — other sessions pushed chat-history-caching and
  mobile auth/redirect features in the meantime); fetched, confirmed no
  real line-level overlap with my files, merged cleanly (stashing and then
  restoring one other session's blocking uncommitted rename first), then
  pushed successfully. Backend: `db2b232` → merged as `e6f7f3c`. UI:
  `4d3d833` → merged as `686b6cf`. Wrote up the general procedure as
  [[knowledge/references/concurrent-sessions-shared-repo]] since this repo
  is worked on by multiple concurrent sessions as a standing condition, not
  a one-off.

## Still open
- `NotificationBell` only mounts on the two Overview pages (brand/creator),
  not a shared layout — live subscription (and the badge) only stays live
  while on Overview. User hasn't decided whether to lift it into a shared
  context for app-wide live updates.
- RLS-disabled advisory above — needs user decision on policies before
  enabling RLS on those 2 tables.
